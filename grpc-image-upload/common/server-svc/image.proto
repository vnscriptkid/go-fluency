syntax = "proto3";

package image_proto;

message Image {
    bytes data = 1;
    string name = 2;
}

message ImageResponse {
    string message = 1;
}

service ImageService {
    rpc UploadImage(stream Image) returns (ImageResponse);
}